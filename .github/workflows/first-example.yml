name: my first CICD implementation

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout-repo
              uses: actions/checkout@v6

            - name: welcome message
              run: echo "My first Github Action Job"

            - name: list files
              run: ls

            - name: read file
              run: cat README.md